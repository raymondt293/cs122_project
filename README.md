To run the program, you will need to import Pygame.
To install Pygame, you can use pip.
Run the following pip install command: pip install pygame
Verify that Pygame is installed by running: import pygame
